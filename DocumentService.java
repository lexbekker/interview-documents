package ru.bekker.interview_tasks.documents;

public class DocumentService {

    public void precise(Document document) {
        switch(document.type) {
            case PDF -> {
                // PDF logic
            }
            case XML -> {
                // XML Logic
            }
            case DOCX -> {
                // DOCX logic
            }
        }
    }

    public void sign(Document document) {
        switch(document.type) {
            case PDF -> {
                // PDF logic
            }
            case XML -> {
                // XML Logic
            }
            case DOCX -> {
                // DOCX logic
            }
        }
    }
}
